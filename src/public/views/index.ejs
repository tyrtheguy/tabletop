<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }

        #token-panel {
            width: 320px;
            height: 180px;
            position: absolute;
            background: #ffffff;
            visibility: hidden;
            font-size: 36px;
            width: fit-content;
            height: fit-content;
            padding: 20px;
            border-radius: 5%;
        }

        #token-getter {
            font-size: 48px;
        }

        .token-linker {
            cursor: pointer;
        }

        .map-getter {
            font-size: 48px;
            cursor: pointer;
        }
    </style>
    <link rel='icon' href='/static/favicon.ico'>
    <title>Tabletop - BETA</title>
</head>
<body>
    <div id="mapContainer">
        <div id='token-panel'>
            <label id="panel-token-name"></label><br>
            <label id='panel-token-hp'></label>
        </div>
        <canvas id="gameCanvas"></canvas>
    </div>
    <div id='token-getter'>
        <label>Characters Scale</label>
        <input min='0' max='200' type='range' id='characters-scales'>
    </div>
    <hr>
    <div id='map-getter'>
        
    </div>

    <script src="/socket.io/socket.io.js"></script>

    <!-- Loading the socket -->
    <script> 

    </script>
    <script src='/static/scripts/index.js' type="text/javascript"></script>
</body>
</html>